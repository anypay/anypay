
import * as assert from 'assert'

describe("Products", () => {

  describe("Importing via API", () => {
    it.skip("should import new items")
    it.skip("should update existing items")
  })

  describe("Importing from CSV", () => {
    it.skip("should sync the catalog via API and add new items")
    it.skip("should update discount price data")
  })

  describe("Importing From eGifter", () => {
    it.skip("should sync the catalog via API and add new items")
    it.skip("should sync the catalog via API and deprecate missing items")
    it.skip("should update discount price data")
  })

  describe("Importing From Square", () => {
    it.skip("should sync the catalog and add new items")
    it.skip("should sync the catalog and deprecate missing items")
  })

  describe("Importing From Clover", () => {
    it.skip("should sync the catalog and add new items")
    it.skip("should sync the catalog and deprecate missing items")
  })

  describe("Importing From Acadia", () => {
    it.skip("should connect to the Acadia POS API") 
    it.skip("should mark products as part of 'acadai' catalog")
  })

})

